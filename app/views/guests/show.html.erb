<h3><%= @guest.first_name %> <%= @guest.last_name %></h3>  
<p>Age: <%= @guest.age %> | <%= @guest.gender %></p>

<div>
	<p>
		<% x = @guest.id %>
		<%= link_to('Edit', :action => 'edit', :id => x) %> | 
		<%= link_to 'Delete', "/guests/#{@guest.id}",
								method: :delete, 
								data: { confirm: 'Do you really want to delete this guest?' } %>
	</p>
</div>

<br />
<h4><%= link_to 'Add a new activity', new_activity_path %>  |  <%= link_to 'View all activities', activities_path %><br>
<h4><%= link_to 'Add a new guest', new_guest_path %>  |  <%= link_to 'View all guests', guests_path %><br>
<h4><%= link_to 'Add a new reservation', new_reservation_path %>  |  <%= link_to 'View all reservations', reservations_path %></h4>
</h4>
<br />
<%= params.inspect %>
<br />
<h1>Guests#show</h1>
<p>Find me in app/views/guests/show.html.erb</p>